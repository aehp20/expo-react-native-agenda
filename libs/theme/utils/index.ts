export const getThemeCookie = (
  defaultTheme: string,
  enabledResetTheme?: boolean,
) => {
  return defaultTheme;
};

export const setThemeCookie = (theme: string) => {
  // Set the theme in Cookies
  // Example Cookies.set(THEME_COOKIE_NAME, theme, { expires: 365, path: '/' });
};
